# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: resume-builder.spec.ts >> Resume Builder E2E Tests >> should verify layout split and basic elements visibility
- Location: tests\resume-builder.spec.ts:15:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Resume Editor')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('Resume Editor')
    3 × waiting for" http://127.0.0.1:3000/resume" navigation to finish...
      - navigated to "http://127.0.0.1:3000/resume"

```

```yaml
- heading "File not found" [level=1]
- paragraph: The file "d:\Rixora\resume" cannot be found. It may have been moved, edited, or deleted.
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Resume Builder E2E Tests', () => {
  4  |   test.beforeEach(async ({ page }, testInfo) => {
  5  |     const baseURL = testInfo.project.use.baseURL ?? 'http://127.0.0.1:3000';
  6  | 
  7  |     await page.goto(`${baseURL}/resume`, { waitUntil: 'domcontentloaded' });
  8  |     await page.waitForLoadState('domcontentloaded');
  9  | 
  10 |     if (page.url().includes('/sign-in')) {
  11 |       test.skip(true, 'Skipping resume builder checks while Clerk redirects unauthenticated users to sign-in');
  12 |     }
  13 |   });
  14 | 
  15 |   test('should verify layout split and basic elements visibility', async ({ page }) => {
> 16 |     await expect(page.getByText('Resume Editor')).toBeVisible();
     |                                                   ^ Error: expect(locator).toBeVisible() failed
  17 |     await expect(page.getByText('Preview')).toBeVisible();
  18 |   });
  19 | 
  20 |   test('should test resume improvement mock AI responses and ATS score updates', async ({ page }) => {
  21 |     await page.route('**/improveWithAI', async route => {
  22 |       await route.fulfill({
  23 |         status: 200,
  24 |         contentType: 'application/json',
  25 |         body: JSON.stringify({ success: true, text: 'Improved response from Mock AI.' }),
  26 |       });
  27 |     });
  28 | 
  29 |     await page.route('**/getAtsScore', async route => {
  30 |       await route.fulfill({
  31 |         status: 200,
  32 |         contentType: 'application/json',
  33 |         body: JSON.stringify({ score: 95, feedback: 'Excellent mock ATS feedback.', missingKeywords: [] }),
  34 |       });
  35 |     });
  36 | 
  37 |     await expect(page.getByText('Resume Editor')).toBeVisible();
  38 |     await expect(page.getByText('Preview')).toBeVisible();
  39 |   });
  40 | });
  41 | 
```