# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth-flow.spec.ts >> Authentication Middleware Interception >> should redirect unauthenticated request to sign-in from protected path: /resume
- Location: tests\auth-flow.spec.ts:7:9

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 307
Received: 404
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Authentication Middleware Interception', () => {
  4  |   const protectedRoutes = ['/dashboard', '/resume', '/interview'];
  5  | 
  6  |   for (const route of protectedRoutes) {
  7  |     test(`should redirect unauthenticated request to sign-in from protected path: ${route}`, async ({ page }, testInfo) => {
  8  |       const baseURL = testInfo.project.use.baseURL ?? 'http://127.0.0.1:3000';
  9  |       const response = await page.request.fetch(`${baseURL}${route}`, { maxRedirects: 0 });
  10 | 
> 11 |       expect(response.status()).toBe(307);
     |                                 ^ Error: expect(received).toBe(expected) // Object.is equality
  12 |       expect(response.headers()['location']).toContain('/sign-in');
  13 |     });
  14 |   }
  15 | });
  16 | 
```